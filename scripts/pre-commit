#!/usr/bin/env bash
set -e

echo "=== Pre-commit: typecheck ==="
npx tsc --noEmit

echo "=== Pre-commit: lint ==="
npx eslint .

echo "=== Pre-commit: tests ==="
npx vitest run

echo "=== All checks passed ==="
